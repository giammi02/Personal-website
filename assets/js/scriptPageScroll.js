
$(function() {
  $.scrollify({
      section : ".section",
      easing: "easeOutExpo",
      scrollSpeed: 600,
  });
});


/*
$('.link').click(function(){
    var moveTo = $(this).attr('href');
    $.scrollify("move",moveTo);
  });
  */