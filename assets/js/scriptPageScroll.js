
$(function() {
  $.scrollify({
      section : ".section",
      easing: "easeOutBounce",
      scrollSpeed: 100,
      touchScroll: false,
  });
});


/*
$('.link').click(function(){
    var moveTo = $(this).attr('href');
    $.scrollify("move",moveTo);
  });
  */