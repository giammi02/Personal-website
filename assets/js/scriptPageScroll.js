
$(function() {
  $.scrollify({
      section : ".section",
      easing: "easeOutExpo",
      scrollSpeed: 100,
  });
});


/*
$('.link').click(function(){
    var moveTo = $(this).attr('href');
    $.scrollify("move",moveTo);
  });
  */