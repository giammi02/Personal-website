
$(function() {
  $.scrollify({
      section : ".section",
      easing: "easeOutCubic",
      scrollSpeed: 600,
  });
});


/*
$('.link').click(function(){
    var moveTo = $(this).attr('href');
    $.scrollify("move",moveTo);
  });
  */